Created VS Code configuration files in directory
Created tasks.json

Created launch.json

Created both VS Code configuration files:

.vscode/tasks.json - Includes two tasks:

Run Python App: Executes app.py using the Python interpreter
Install Dependencies: Installs packages from requirements.txt
.vscode/launch.json - Includes a debug configuration:

Python: app.py: Launches the app for debugging with integrated terminal output
You can now run the app using Ctrl+Shift+B (Run Build Task) or debug it with F5.